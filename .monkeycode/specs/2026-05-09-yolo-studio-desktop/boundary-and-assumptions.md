# 标炬（LabelTorch）边界与假设

## MVP 边界

1. 仅单用户离线。
2. 仅 YOLO txt 完整支持。
3. 仅 bbox 与辅助推理闭环。
4. 仅 Windows 绿色包正式交付。

## 关键假设

1. 用户具备 NVIDIA 驱动基础环境。
2. 用户数据目录读写权限正常。
3. 同一次训练数据分辨率一致。

## 非目标

1. 不做 SaaS 平台。
2. 不做团队协作权限。
3. 不做在线模型服务托管。

## 兼容策略

1. 提供 CPU/CUDA 双包。
2. 对 CUDA 不匹配场景自动降级 CPU。
